// todo: make OwinContext.ctor() public for this to work. 
// todo: solve handling breaking changes?

to make this work 
- build Simple.Owin +0.0.1 locally
- remove TcpServer & Simple.Owin package  from demo
- add TcpServer package you just built (add nuget source to artifacts folder is easiest)